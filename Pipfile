[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pyyaml = ">=6.0"
tomli-w = ">=1.0"

[dev-packages]
pytest = ">=7.0"
pytest-cov = ">=4.0"
flake8 = ">=6.0"
black = ">=23.0"
isort = ">=5.12"
mypy = ">=1.0"
pre-commit = ">=3.0"
types-pyyaml = ">=6.0"

[requires]
python_version = "3.10"

[scripts]
test = "pytest"
test-cov = "pytest --cov=cascconf --cov-report=html"
lint = "flake8 cascconf tests"
format = "black cascconf tests"
format-check = "black --check cascconf tests"
sort-imports = "isort cascconf tests"
sort-imports-check = "isort --check-only cascconf tests"
typecheck = "mypy cascconf"
